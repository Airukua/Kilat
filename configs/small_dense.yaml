experiment:
  name: "kilat-small-dense-finetune"
  description: "Fine-tuning on downstream task with dense FFN"
  tags: ["fine-tuning", "dense", "small"]

model:
  # Core architecture
  vocab_size: 50000
  n_embd: 640     
  n_layer: 8
  n_head: 10
  
  # Attention
  recall_ratio: 0.6
  latent_dim: null 
  attn_drop: 0.1
  
  # Feed-forward (dense)
  ffn_mode: dense
  ff_mult: 2.67
  ffn_dropout: 0.1
  
  # Regularisation
  embd_drop: 0.1
  resid_drop: 0.1
  
  # Token IDs
  pad_token_id: 0
  bos_token_id: 1
  eos_token_id: 2
  
  # Weight tying
  tie_word_embeddings: true
  use_cache: false
  initializer_range: 0.02

training:
  # I/O
  output_dir: "./checkpoints/kilat-small-dense"
  resume_from_checkpoint: null
  save_checkpoints: true
  
  # Training mode (epoch-based for fine-tuning)
  training_mode: steps
  max_steps: 300000
  
  # Optimisation
  learning_rate: 5.0e-05
  per_device_train_batch_size: 512
  per_device_eval_batch_size: 512
  gradient_accumulation_steps: 2
  weight_decay: 0.01
  max_grad_norm: 1.0
  
  # Schedule
  warmup_steps: 2000
  
  # Logging & evaluation
  logging_steps: 100
  eval_steps: 1000
  save_steps: 5000
  save_total_limit: 5
  
  # Early stopping
  early_stopping_patience: 3
  early_stopping_threshold: 0.0
  
  # Mixed precision
  precision: fp16  # FP16 for GPU, fp32 for CPU
  
  # Reporting
  report_to: "none"
  run_name: "kilat-small-dense-finetune"
  
  # Reproducibility
  seed: 42